# 执行计划：移植上游低风险修复

1. 在当前活动任务中确认上游 ref、工作区状态和目标提交文件清单。
2. 按 `cda19b25`、`3b19a24b`、`f273d301` 顺序应用补丁；每步检查 diff，确保没有越界文件。
3. 运行 Home 请求日志、asset URL、通知音频相关测试；必要时补齐只针对移植行为的测试调整。
4. 运行格式、类型、lint、生成绑定检查和 `git diff --check`；对 Rust target-gated 代码记录 Windows 环境限制。
5. 由质量检查阶段复核 upstream scope、跨层数据流、依赖 target 条件、未跟踪文件和最终提交范围。
6. 只提交本子任务的代码与任务证据；保留其他任务或用户已有修改。
