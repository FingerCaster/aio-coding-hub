# Pi / OMP 界面与操作对齐

日期：2026-09-26。用户体验测试包后要求：供应商页参照 Claude/Codex，CLI 管理参照其他渠道，并由 AGY 与 Codex 协作实施。

## 分工

- AGY（Orca task_c9d995e19ff4 / ctx_1584fbd3732d）：src/pages/providers/native 的卡片、工具栏、表单、目标选择、网关区与测试。
- Codex：CLI 管理的安装状态、目录操作、目标切换、官方安装指引、供应商/网关直达，以及整体视觉、交互、回归和测试包验收。
- 复用既有 Card、Button、Dialog、NativeTargetPicker、Query 和受控 desktop opener，保持界面一致。

## 行为边界

独立 AIO 入口与原生配置并存；加入入口不等于使用中，不自动切默认模型。未知/损坏/只读配置继续禁写，保留 revision/节点所有权检查、原文未知字段、目标切换隔离。探测不启动 CLI；没有版本元数据时明确显示未知。

CLI 页新增真实可用操作：刷新、打开配置目录、复制路径、默认/自定义/Profile 目标选择、原生供应商与网关直达、官方文档、安装/更新命令复制、官方发布页。安装命令不会自动执行。未实装的登录、会话、WSL、MCP/Skills 和自动更新不伪装为可用开关。

## 验收

- 前端：已安装/未安装/元数据缺失、探测失败/目标失败、刷新后失败不展示过期正常状态、长路径和窄窗口、目标切换、复制/打开与导航。
- 供应商：搜索/状态筛选、新建/编辑/移除/档案、原文编辑、网关预览/导入与只读状态回归。
- typecheck、lint、相关测试、生产构建；浏览器使用隔离 fixture，不读写用户凭证。
- 更新本地测试包及源码指纹；不提交、推送或发布。

## 2026-09-26 验收结果

- AGY 已完成供应商组件改版，Codex 已审查整合；协作 worker 已释放，未留待回收终端。
- 全量前端单测 323 个文件、2,992 项通过；本轮相关 9 个文件、46 项通过。typecheck、lint 通过。
- 补充了未知状态不计入未加入、查询失败不展示缓存正常状态、目录与渠道切换隔离、URL 返回/前进及移除参数回归。
- Edge 实际组件验收覆盖浅色/深色、760px 窄窗口、空态、只读、搜索/状态筛选、新增/编辑和网关切换；使用隔离 fixture，未读取用户凭证。
- 修复网关入口卡片被 flex 压缩导致按钮裁切；1320px 与 760px 两档均确认卡片无裁切、页面无横向溢出。浏览器证据位于 .local/ui-review/。
- 本轮仅改前端交互与文档，沿用已验证的原生配置及网关契约。生产构建与测试包校验记录保存在 .local/test-builds/，最终产物索引为 latest-pi-omp-package.json。
- 当前保持未提交状态，等待用户体验验收；未推送或发布。
